(module feed (generate-feed generate-archive-index)
  (import scheme
	  (chicken base)
	  (chicken format)
	  (chicken file)
	  (chicken io)
	  (chicken process)
	  (chicken pathname)
	  (chicken irregex)
	  (chicken sort)
	  srfi-1
	  srfi-13

	  utils
	  md-parser
	  )


  )
